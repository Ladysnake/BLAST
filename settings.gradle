pluginManagement {
	repositories {
		gradlePluginPortal()
		mavenCentral()
		maven {
			name = "FabricMC"
			url = "https://maven.fabricmc.net"
		}
		maven {
			name = "RelativityMC"
			url = "https://repo.codemc.io/repository/relativitymc"
		}
	}
}

rootProject.name = "blast"

includeBuild("build-logic")
["common", "fabric", "neoforge"]
//["common", "fabric"]
		.each {
			include(it)
			project(":$it").name = "${rootProject.name}-$it"
		}
